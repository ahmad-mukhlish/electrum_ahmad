appId: com.electrum.ahmad.electrum_ahmad
---
# Bikes Assert List Flow
# Purpose: Reusable assertion that bike list is showing with bikes

# Assert Bike section is visible
- assertVisible:
    text: ".*Bike section.*"

# Assert at least one Bike card is visible
- assertVisible:
    text: ".*Bike card.*"
