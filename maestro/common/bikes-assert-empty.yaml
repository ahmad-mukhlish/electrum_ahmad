appId: com.electrum.ahmad.electrum_ahmad
---
# Bikes Assert Empty Flow
# Purpose: Reusable assertion that empty state is showing

# Assert Bike empty (empty state widget) is visible
- assertVisible:
    text: ".*Bike empty.*"

# Assert Bike card is NOT visible
- assertNotVisible:
    text: ".*Bike card.*"
